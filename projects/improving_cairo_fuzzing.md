# Improving oss-fuzz cairo fuzzing

I noticed that the fuzz coverage for cairo was abysmal and I decided to create a better fuzzer for cairo...

As of writing this, the current fuzzer only exercises roughly 30 percent of the codebase which I thought was a bit odd...


